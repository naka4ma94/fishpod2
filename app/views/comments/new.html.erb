<h1>Comments#new</h1>

<%= form_with model: [@post, @comment], locale: true do |f| %>
  <%= f.label :comment %>
  <%= f.text_area :content %>
  <%= f.hidden_field :post_id, value: @post.id %>
  <%= f.submit %>
<% end %>
